/**
 * Recording Widget Renderer Script
 * 
 * This is a minimal renderer entry point for webpack bundling.
 * The actual logic is in the HTML inline script for simplicity,
 * but this file is required for the webpack entry point.
 */

// The recording widget uses inline script in the HTML
// This file exists to satisfy webpack's entry point requirements
console.log('[RecordingWidgetRenderer] Renderer script loaded');
